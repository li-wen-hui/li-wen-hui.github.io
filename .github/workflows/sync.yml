name: Sync Academic Data
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "sync enabled"
